(defun empty ())

(defun only-args (a b))

(defun add-it (a b)
  (+ a b))

(defun add-sequential (a b)
  (+ a b)
  (+ a b))

(defun add-recursive (a b)
  (+ (+ a b) b))

(defun add-mul (a b)
  (+ (* a b) b))

(defun and-it (a b)
  (and a b))

(defun and-add (a b)
  (and (+ a b) b)
  (* a b))

(defun if-it (a b c)
  (if (< a b) (+ b c) c))
